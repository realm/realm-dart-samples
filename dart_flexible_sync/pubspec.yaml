name: tasktracker
description: MongoDB flexible sync example.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  realm_dart: ^0.4.0+beta
  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
